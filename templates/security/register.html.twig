<h1>Inscription</h1>

{{ form_start(registrationForm) }}
    {{ form_row(registrationForm.email, {'label': 'Adresse email'}) }}
    {{ form_row(registrationForm.username, {'label': 'Username'}) }}
    {{ form_row(registrationForm.password.first, {'label': 'Mot de passe'}) }}
    {{ form_row(registrationForm.password.second, {'label': 'Confirmez le mot de passe'}) }}
<button type="submit">S'inscrire</button>
{{ form_end(registrationForm) }}
